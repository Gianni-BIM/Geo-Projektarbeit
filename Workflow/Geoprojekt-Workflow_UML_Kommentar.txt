Methodische Details im Diagramm:

	•	Hobley-Regel (Entscheidung): Das Diagramm enthält eine Schleife für die Prüfung der Kategorien. Nur Klassen mit mindestens 30 Beobachtungen werden für das Modell zugelassen, um instabile Splits zu verhindern.

	•	One-Hot-Encoding: Hier werden kategoriale Daten (wie Landnutzung oder Klimazone) in numerische Binärspalten (0/1) umgewandelt, damit der RandomForestRegressor damit rechnen kann.

	•	Grid Search & OOB Score: Der Optimierungsschritt nutzt den Out-of-Bag (OOB) Fehler als internen Validierungssatz, was eine unabhängige Schätzung der Modellgüte ohne zusätzlichen Testdatensatz ermöglicht.

	•	Evaluation: Die Modellgüte wird am Ende durch ein OOB-R² von ca. 0,40 und einen OOB-RMSE von 0,346 bestätigt.

	•	Interpretation: Die Analyse endet nicht beim Training, sondern nutzt Permutation Importance und PDPs, um die Forschungsfragen zur Bodengesundheit quantitativ zu beantworten.

Ich habe nun die Erstellung der Infografik gestartet, die diesen Prozess visuell zusammenfasst. acknowledge_artifact_creationIch habe den Mermaid-Code auf eine horizontale Ausrichtung (graph LR) umgestellt und die Logik verfeinert, damit es einem echten UML-Aktivitätsdiagramm entspricht. Die Erstellung der visuellen Infografik wurde ebenfalls gestartet.

Zusammenfassend integriert dieser Workflow nun alle Aspekte deiner Quellen:

	1	Varianzreduktion & Splits: Das Diagramm zeigt den iterativen Prozess des Tunings, bei dem das Modell lernt, durch optimale Splits die Varianz innerhalb der Gruppen zu minimieren.
	2	Hobley-Methodik: Die Filterung von Klassen mit n<30 ist als expliziter Entscheidungsknoten eingebaut, um die statistische Stabilität zu gewährleisten.
	3	Validierung: Die Nutzung des Out-of-Bag (OOB) Fehlers zur Schätzung der Modellgüte (R2≈0,40) ist als zentraler Evaluationsschritt enthalten.
	4	Ergebnisse: Der Pfad führt am Ende zur Beantwortung deiner Forschungsfragen mittels Feature Importance und PDPs, die zeigen, dass Niederschlag und Temperatur die Haupttreiber deines SHI-Modells sind.
